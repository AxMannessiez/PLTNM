import {
  getCurrentPlaylistData,
  removeCurrentPlaylistData,
  storeCurrentPlaylistData,
} from './currentPlaylistData';
import { getGameId, removeGameId, storeGameId } from './gameId';
import {
  getRedirectAfterSignIn,
  removeRedirectAfterSignIn,
  storeRedirectAfterSignIn,
} from './redirectAfterSignIn';
import removeAll from './removeAll';
import { getTeamId, removeTeamId, storeTeamId } from './teamId';
import { getUserId, removeUserId, storeUserId } from './userId';
import { getUserName, removeUserName, storeUserName } from './userName';

export {
  getCurrentPlaylistData,
  removeCurrentPlaylistData,
  storeCurrentPlaylistData,
};
export { getGameId, removeGameId, storeGameId };
export {
  getRedirectAfterSignIn,
  removeRedirectAfterSignIn,
  storeRedirectAfterSignIn,
};
export { removeAll };
export { getTeamId, removeTeamId, storeTeamId };
export { getUserId, removeUserId, storeUserId };
export { getUserName, removeUserName, storeUserName };
